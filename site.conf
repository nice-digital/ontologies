map $http_accept $ext {
  default	".html";
  "~*ttl"	".ttl";
}

server {
  listen 80 default_server;
  root /var/www/html;
  server_name _;

  location /ns {
    autoindex on;
    add_header Vary Accept;
    try_files $uri$ext $uri $uri/ =404;
  }
}
